<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="PINView.Maui.PINView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:helpers="clr-namespace:PINView.Maui.Helpers"
    xmlns:maui="clr-namespace:PINView.Maui"
    x:DataType="maui:PINView">
    <ContentView.Content>
        <Grid HorizontalOptions="Start">
            <HorizontalStackLayout
                x:Name="PINBoxContainer"
                AutomationProperties.ExcludedWithChildren="True"
                AutomationProperties.IsInAccessibleTree="False"
                Spacing="{x:Static helpers:Constants.DefaultBoxSpacing}" />

            <!--  This Textbox stays on top of the control, and is what the user interacts with  -->
            <maui:HiddenPinEntry
                x:Name="hiddenTextEntry"
                AutomationProperties.IsInAccessibleTree="true"
                HeightRequest="{Binding Height, Source={x:Reference PINBoxContainer}, x:DataType=HorizontalStackLayout}"
                HorizontalOptions="Center"
                Keyboard="Numeric"
                MaxLength="{Binding PINLength, Source={RelativeSource AncestorType={x:Type ContentView}, AncestorLevel=1}}"
                SemanticProperties.Description="{Binding SemanticDescription, Source={RelativeSource AncestorType={x:Type ContentView}, AncestorLevel=1}}"
                SemanticProperties.Hint="{Binding SemanticHint, Source={RelativeSource AncestorType={x:Type ContentView}, AncestorLevel=1}}"
                WidthRequest="{Binding Width, Source={x:Reference PINBoxContainer}, x:DataType=HorizontalStackLayout}" />
        </Grid>
    </ContentView.Content>
</ContentView>
